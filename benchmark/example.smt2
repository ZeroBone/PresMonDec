(set-info :smt-lib-version 2.6)
(set-logic QF_LIA)
(set-info :category "random")
(set-info :status sat)
(declare-fun x () Int)
(declare-fun y () Int)
(assert (= (- x y) (+ x (- y) 1)))
(check-sat)
(exit)
